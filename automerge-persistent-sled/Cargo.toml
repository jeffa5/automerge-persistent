[package]
name = "automerge-persistent-sled"
version = "0.1.0"
authors = ["Andrew Jeffery <dev@jeffas.io>"]
edition = "2018"

[dependencies]
automerge = { git = "https://github.com/automerge/automerge-rs", branch = "send" }
automerge-protocol = { git = "https://github.com/automerge/automerge-rs", branch = "send" }
automerge-persistent = { path = "../automerge-persistent" }
sled = "0.34.6"
thiserror = "1.0.24"
async-trait = "0.1.50"
futures = "0.3.15"

[dev-dependencies]
criterion = "0.3.4"
automerge-backend = { git = "https://github.com/automerge/automerge-rs", branch = "send" }

[[bench]]
name = "save"
harness = false
